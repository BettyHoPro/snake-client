// const net = require('net');

const ob = require('./client');
const { setupInput }  = require('./input');
console.log('Connecting ...');

// connect();
setupInput(ob.cat());  //ob.cat call a function   //ob.cat just pass the function into a setup input, 



